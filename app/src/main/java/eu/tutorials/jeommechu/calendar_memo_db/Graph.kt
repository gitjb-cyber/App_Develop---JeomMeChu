package eu.tutorials.jeommechu.calendar_memo_db

import android.content.Context
import androidx.room.Room
/*

object Graph {
    lateinit var database: MemoDatabase

    val memoRepository by lazy {
        MemoRepository(memoDao = database.memoDao())
    }

    fun provide(context: Context){
        database = Room.databaseBuilder(context, MemoDatabase::class.java, "memo.db").build()
    }
}
*/
